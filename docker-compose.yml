services:
  product-scraper:
    build: .
    ports:
      - "8000:8000"
    environment:
      SCRAPER_DATA_DIR: /app/data
      CORS_ALLOW_ORIGINS: "*"
    volumes:
      - scraper-data:/app/data
    restart: unless-stopped

volumes:
  scraper-data:
